# Makefile.in generated by automake 1.11.1 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008, 2009  Free Software Foundation,
# Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
subdir = src/main/perl/lib
DIST_COMMON = $(dist_noinst_DATA) $(srcdir)/Makefile.am \
	$(srcdir)/Makefile.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/autotools/ads_PERL_INCLUDES.m4 \
	$(top_srcdir)/autotools/ads_PERL_LIBDIR.m4 \
	$(top_srcdir)/autotools/ads_PERL_MODULE.m4 \
	$(top_srcdir)/autotools/ads_PROG_PERL.m4 \
	$(top_srcdir)/autotools/ax_distcheck_hack.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__installdirs = "$(DESTDIR)$(perl5bedrockapachelibdir)" \
	"$(DESTDIR)$(perl5bedrockapplicationlibdir)" \
	"$(DESTDIR)$(perl5bedrockcontextlibdir)" \
	"$(DESTDIR)$(perl5bedrockhtmllibdir)" \
	"$(DESTDIR)$(perl5bedrockinputsourcelibdir)" \
	"$(DESTDIR)$(perl5bedrocklibdir)" \
	"$(DESTDIR)$(perl5bedrocktextlibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxlibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)" \
	"$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)" \
	"$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)" \
	"$(DESTDIR)$(perl5bedrockxmllibdir)" \
	"$(DESTDIR)$(perl5blmfilterlibdir)" \
	"$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)" \
	"$(DESTDIR)$(perl5blmlibdir)" \
	"$(DESTDIR)$(perl5blmstartuplibdir)" \
	"$(DESTDIR)$(perl5blmstartupsessionlibdir)" \
	"$(DESTDIR)$(perl5libdir)"
DATA = $(dist_noinst_DATA) $(perl5bedrockapachelib_DATA) \
	$(perl5bedrockapplicationlib_DATA) \
	$(perl5bedrockcontextlib_DATA) $(perl5bedrockhtmllib_DATA) \
	$(perl5bedrockinputsourcelib_DATA) $(perl5bedrocklib_DATA) \
	$(perl5bedrocktextlib_DATA) $(perl5bedrocktexttagxlib_DATA) \
	$(perl5bedrocktexttagxtaglib_DATA) \
	$(perl5bedrocktexttagxtagnobodylib_DATA) \
	$(perl5bedrocktexttagxtagwithbodyiflib_DATA) \
	$(perl5bedrocktexttagxtagwithbodylib_DATA) \
	$(perl5bedrocktexttagxtagwithbodylooplib_DATA) \
	$(perl5bedrocktexttagxtermlib_DATA) \
	$(perl5bedrockxmlcontainerlib_DATA) $(perl5bedrockxmllib_DATA) \
	$(perl5blmfilterlib_DATA) \
	$(perl5blmindexedtablehandlerlib_DATA) $(perl5blmlib_DATA) \
	$(perl5blmstartuplib_DATA) $(perl5blmstartupsessionlib_DATA) \
	$(perl5lib_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
pkglibdir = @pkglibdir@
A2ENSITE = @A2ENSITE@
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BASH_SH = @BASH_SH@
BEDROCK_RELEASE = @BEDROCK_RELEASE@
CAT = @CAT@
CHGRP = @CHGRP@
CHOWN = @CHOWN@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DFLT_MAINTAINER = @DFLT_MAINTAINER@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
FIND = @FIND@
GREP = @GREP@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
MKDIR_P = @MKDIR_P@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PERL = @PERL@
PERL_LIBDIR = @PERL_LIBDIR@
POD2MAN = @POD2MAN@
RM = @RM@
RMDIR = @RMDIR@
SED = @SED@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
am__leading_dot = @am__leading_dot@
am__tar = @am__tar@
am__untar = @am__untar@
apache_default_htdocsdir = @apache_default_htdocsdir@
apache_layout = @apache_layout@
apache_sessiondir = @apache_sessiondir@
ax_perl5_extra_includes = @ax_perl5_extra_includes@
ax_perl5_extra_includes_opt = @ax_perl5_extra_includes_opt@
bedrock_perl5_inc_begin_block_snippet_filepath = @bedrock_perl5_inc_begin_block_snippet_filepath@
bindir = @bindir@
build_alias = @build_alias@
builddir = @builddir@
cgibindir = @cgibindir@
configdir = @configdir@
cssdir = @cssdir@
datadir = @datadir@
datarootdir = @datarootdir@
do_subst_command = @do_subst_command@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host_alias = @host_alias@
htdocsdir = @htdocsdir@
htmldir = @htmldir@
imgdir = @imgdir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
pebblesdir = @pebblesdir@
perl5_pkgdatadir = @perl5_pkgdatadir@
perl5_pkglibdir = @perl5_pkglibdir@
perl5_pkgvdatadir = @perl5_pkgvdatadir@
perl5_pkgvlibdir = @perl5_pkgvlibdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
perl5libdir = @PERL_LIBDIR@
perl5bedrocklibdir = $(perl5libdir)/Bedrock
perl5bedrockhtmllibdir = $(perl5libdir)/Bedrock/HTML
perl5bedrockinputsourcelibdir = $(perl5libdir)/Bedrock/InputSource
perl5bedrockapachelibdir = $(perl5libdir)/Bedrock/Apache
perl5bedrockapplicationlibdir = $(perl5libdir)/Bedrock/Application
perl5bedrockcontextlibdir = $(perl5libdir)/Bedrock/Context
perl5bedrocktextlibdir = $(perl5libdir)/Bedrock/Text
perl5bedrocktexttagxlibdir = $(perl5libdir)/Bedrock/Text/TagX
perl5bedrocktexttagxtaglibdir = $(perl5libdir)/Bedrock/Text/TagX/TAG
perl5bedrocktexttagxtagnobodylibdir = $(perl5libdir)/Bedrock/Text/TagX/TAG/NoBody
perl5bedrocktexttagxtagwithbodylibdir = $(perl5libdir)/Bedrock/Text/TagX/TAG/WithBody
perl5bedrocktexttagxtagwithbodyiflibdir = $(perl5libdir)/Bedrock/Text/TagX/TAG/WithBody/If
perl5bedrocktexttagxtagwithbodylooplibdir = $(perl5libdir)/Bedrock/Text/TagX/TAG/WithBody/Loop
perl5bedrocktexttagxtermlibdir = $(perl5libdir)/Bedrock/Text/TagX/Term
perl5bedrockxmllibdir = $(perl5libdir)/Bedrock/XML
perl5bedrockxmlcontainerlibdir = $(perl5libdir)/Bedrock/XML/Container
perl5blmlibdir = $(perl5libdir)/BLM
perl5blmfilterlibdir = $(perl5libdir)/BLM/Filter
perl5blmindexedtablehandlerlibdir = $(perl5libdir)/BLM/IndexedTableHandler
perl5blmstartuplibdir = $(perl5libdir)/BLM/Startup
perl5blmstartupsessionlibdir = $(perl5libdir)/BLM/Startup/Session
perl5lib_DATA = \
    Bedrock.pm \
    bedrock_handler_common.pm

perl5bedrocklib_DATA = \
    Bedrock/BedrockConfig.pm \
    Bedrock/Context.pm \
    Bedrock/BedrockCGI.pm \
    Bedrock/VERSION.pm \
    Bedrock/XML.pm \
    Bedrock/Array.pm \
    Bedrock/BufferStream.pm \
    Bedrock/Dump.pm \
    Bedrock/Handle.pm \
    Bedrock/Handler.pm \
    Bedrock/Hash.pm \
    Bedrock/InputSource.pm \
    Bedrock/BedrockJSON.pm \
    Bedrock/Log.pm \
    Bedrock/Plugin.pm \
    Bedrock/Template.pm

BEDROCK = $(perl5bedrocklib_DATA:.pm=.pm.in)
perl5bedrockapplicationlib_DATA = \
    Bedrock/Application/Plugin.pm

BEDROCK_APPLICATION = $(perl5bedrockapplicationlib_DATA:.pm=.pm.in)
perl5bedrockapachelib_DATA = \
    Bedrock/Apache/Bedrock.pm \
    Bedrock/Apache/bedrock.pl \
    Bedrock/Apache/bedrock_sh.pl \
    Bedrock/Apache/Request_cgi.pm \
    Bedrock/Apache/Request_shell.pm \
    Bedrock/Apache/BedrockSessionFiles.pm \
    Bedrock/Apache/BedrockDocs.pm

BEDROCK_APACHE = $(perl5bedrockapachelib_DATA:.pm=.pm.in)
perl5bedrockcontextlib_DATA = \
    Bedrock/Context/Shell.pm

BEDROCK_CONTEXT = $(perl5bedrockcontextlib_DATA:.pm=.pm.in)
perl5bedrockhtmllib_DATA = \
    Bedrock/HTML/Helper.pm

BEDROCK_HTML = $(perl5bedrockhtmllib_DATA:.pm=.pm.in)
perl5blmfilterlib_DATA = \
    BLM/Filter/crypt.pm \
    BLM/Filter/csv.pm \
    BLM/Filter/xls.pm

BEDROCK_FILTER = $(perl5blmfilterlib_DATA:.pm=.pm.in)
perl5bedrockinputsourcelib_DATA = \
    Bedrock/InputSource/Term.pm

BEDROCK_INPUTSOURCE = $(perl5bedrockinputsourcelib_DATA:.pm=.pm.in)
perl5bedrocktextlib_DATA = \
    Bedrock/Text/TagX.pm \
    Bedrock/Text/URLEncode.pm

BEDROCK_TEXT = $(perl5bedrocktextlib_DATA:.pm=.pm.in)
perl5bedrocktexttagxlib_DATA = \
    Bedrock/Text/TagX/Error.pm \
    Bedrock/Text/TagX/Expr.pm \
    Bedrock/Text/TagX/Func.pm \
    Bedrock/Text/TagX/Log.pm \
    Bedrock/Text/TagX/Output.pm \
    Bedrock/Text/TagX/Parser.pm \
    Bedrock/Text/TagX/Scalar.pm \
    Bedrock/Text/TagX/Symtab.pm \
    Bedrock/Text/TagX/TAG.pm \
    Bedrock/Text/TagX/Term.pm

BEDROCK_TEXT_TAGX = $(perl5bedrocktexttagxlib_DATA:.pm=.pm.in)
perl5bedrocktexttagxtaglib_DATA = \
    Bedrock/Text/TagX/TAG/NoBody.pm \
    Bedrock/Text/TagX/TAG/WithBody.pm \
    Bedrock/Text/TagX/TAG/SQLHandler.pm

BEDROCK_TEXT_TAGX_TAG = $(perl5bedrocktexttagxtaglib_DATA:.pm=.pm.in)
perl5bedrocktexttagxtagnobodylib_DATA = \
    Bedrock/Text/TagX/TAG/NoBody/Array.pm \
    Bedrock/Text/TagX/TAG/NoBody/Comment.pm \
    Bedrock/Text/TagX/TAG/NoBody/Plugin.pm \
    Bedrock/Text/TagX/TAG/NoBody/Case.pm \
    Bedrock/Text/TagX/TAG/NoBody/Exec.pm \
    Bedrock/Text/TagX/TAG/NoBody/Hash.pm \
    Bedrock/Text/TagX/TAG/NoBody/Include.pm \
    Bedrock/Text/TagX/TAG/NoBody/Iif.pm \
    Bedrock/Text/TagX/TAG/NoBody/NULL.pm \
    Bedrock/Text/TagX/TAG/NoBody/Open.pm \
    Bedrock/Text/TagX/TAG/NoBody/Pebble.pm \
    Bedrock/Text/TagX/TAG/NoBody/Raise.pm \
    Bedrock/Text/TagX/TAG/NoBody/RecordSet.pm \
    Bedrock/Text/TagX/TAG/NoBody/SQL.pm \
    Bedrock/Text/TagX/TAG/NoBody/SQLCommit.pm \
    Bedrock/Text/TagX/TAG/NoBody/SQLRollback.pm \
    Bedrock/Text/TagX/TAG/NoBody/SQLConnect.pm \
    Bedrock/Text/TagX/TAG/NoBody/SQLTable.pm \
    Bedrock/Text/TagX/TAG/NoBody/Trace.pm \
    Bedrock/Text/TagX/TAG/NoBody/Var.pm

BEDROCK_TEXT_TAGX_TAG_NOBODY = $(perl5bedrocktexttagxtagnobodylib_DATA:.pm=.pm.in)
perl5bedrocktexttagxtagwithbodylib_DATA = \
    Bedrock/Text/TagX/TAG/WithBody/Cache.pm \
    Bedrock/Text/TagX/TAG/WithBody/Catch.pm \
    Bedrock/Text/TagX/TAG/WithBody/If.pm \
    Bedrock/Text/TagX/TAG/WithBody/Unless.pm \
    Bedrock/Text/TagX/TAG/WithBody/Loop.pm \
    Bedrock/Text/TagX/TAG/WithBody/NoExec.pm \
    Bedrock/Text/TagX/TAG/WithBody/PebbleDef.pm \
    Bedrock/Text/TagX/TAG/WithBody/Sink.pm \
    Bedrock/Text/TagX/TAG/WithBody/Snippet.pm \
    Bedrock/Text/TagX/TAG/WithBody/Try.pm

BEDROCK_TEXT_TAGX_TAG_WITHBODY = $(perl5bedrocktexttagxtagwithbodylib_DATA:.pm=.pm.in)
perl5bedrocktexttagxtagwithbodyiflib_DATA = \
    Bedrock/Text/TagX/TAG/WithBody/If/Else.pm

BEDROCK_TEXT_TAGX_TAG_WITHBODY_IF = $(perl5bedrocktexttagxtagwithbodyiflib_DATA:.pm=.pm.in)
perl5bedrocktexttagxtagwithbodylooplib_DATA = \
    Bedrock/Text/TagX/TAG/WithBody/Loop/Foreach.pm \
    Bedrock/Text/TagX/TAG/WithBody/Loop/SQLSelect.pm \
    Bedrock/Text/TagX/TAG/WithBody/Loop/While.pm

BEDROCK_TEXT_TAGX_TAG_WITHBODY_LOOP = $(perl5bedrocktexttagxtagwithbodylooplib_DATA:.pm=.pm.in)
perl5bedrocktexttagxtermlib_DATA = \
    Bedrock/Text/TagX/Term/String.pm \
    Bedrock/Text/TagX/Term/Var.pm

BEDROCK_TEXT_TAGX_TERM = $(perl5bedrocktexttagxtermlib_DATA:.pm=.pm.in)
perl5bedrockxmllib_DATA = \
    Bedrock/XML/Container.pm

BEDROCK_XML = $(perl5bedrockxmllib_DATA:.pm=.pm.in)
perl5bedrockxmlcontainerlib_DATA = \
    Bedrock/XML/Container/Array.pm \
    Bedrock/XML/Container/Object.pm \
    Bedrock/XML/Container/Scalar.pm

BEDROCK_XML_CONTAINER = $(perl5bedrockxmlcontainerlib_DATA:.pm=.pm.in)

# Plugins
# -------
perl5blmindexedtablehandlerlib_DATA = \
    BLM/IndexedTableHandler/RecordSet.pm \
    BLM/IndexedTableHandler/Common.pm

BLM_INDEXEDTABLEHANDLER = $(perl5blmindexedtablehandlerlib_DATA:.pm=.pm.in)
perl5blmlib_DATA = \
    BLM/Awk.pm \
    BLM/Benchmark.pm \
    BLM/Date.pm \
    BLM/DBHandler.pm \
    BLM/FileIO.pm \
    BLM/IndexedTableHandler.pm \
    BLM/Mail.pm \
    BLM/Recycle.pm \
    BLM/FTP.pm \
    BLM/SMTP.pm \
    BLM/Session.pm \
    BLM/Stat.pm \
    BLM/Timenow.pm

BLM = $(perl5blmlib_DATA:.pm=.pm.in)
perl5blmstartuplib_DATA = \
    BLM/Startup/Bedrock.pm \
    BLM/Startup/Config.pm \
    BLM/Startup/Cookie.pm \
    BLM/Startup/Env.pm \
    BLM/Startup/Forum.pm \
    BLM/Startup/Header.pm \
    BLM/Startup/Hitcounter.pm \
    BLM/Startup/Input.pm \
    BLM/Startup/MySQLSession.pm \
    BLM/Startup/SQLiteSession.pm \
    BLM/Startup/PostgreSQLSession.pm \
    BLM/Startup/Output.pm \
    BLM/Startup/Session.pm \
    BLM/Startup/SessionManager.pm \
    BLM/Startup/SQLSession.pm \
    BLM/Startup/UserSession.pm 

BLM_STARTUP = $(perl5blmstartuplib_DATA:.pm=.pm.in)
perl5blmstartupsessionlib_DATA = \
    BLM/Startup/Session/GDBM.pm \
    BLM/Startup/Session/SDBM.pm

BLM_STARTUP_SESSION = $(perl5blmstartupsessionlib_DATA:.pm=.pm.in)
dist_noinst_DATA = \
    Bedrock.pm.in \
    bedrock_handler_common.pm.in \
    $(BEDROCK) \
    $(BEDROCK_APPLICATION) \
    $(BEDROCK_APACHE) \
    $(BEDROCK_CONTEXT) \
    $(BEDROCK_HTML) \
    $(BEDROCK_FILTER) \
    $(BEDROCK_INPUTSOURCE) \
    $(BEDROCK_TEXT) \
    $(BEDROCK_TEXT_TAGX) \
    $(BEDROCK_TEXT_TAGX_TAG) \
    $(BEDROCK_TEXT_TAGX_TAG_NOBODY) \
    $(BEDROCK_TEXT_TAGX_TAG_WITHBODY) \
    $(BEDROCK_TEXT_TAGX_TAG_WITHBODY_IF) \
    $(BEDROCK_TEXT_TAGX_TAG_WITHBODY_LOOP) \
    $(BEDROCK_TEXT_TAGX_TERM) \
    $(BEDROCK_XML) \
    $(BEDROCK_XML_CONTAINER) \
    $(BLM_INDEXEDTABLEHANDLER) \
    $(BLM) \
    $(BLM_STARTUP) \
    $(BLM_STARTUP_SESSION)

SUFFIXES = .tdy .in .pm .pm.in
TIDY = $(dist_noinst_DATA:.pm.in=.pm.in.tdy)
all: all-am

.SUFFIXES:
.SUFFIXES: .tdy .in .pm .pm.in
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign src/main/perl/lib/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign src/main/perl/lib/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):
install-perl5bedrockapachelibDATA: $(perl5bedrockapachelib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockapachelibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockapachelibdir)"
	@list='$(perl5bedrockapachelib_DATA)'; test -n "$(perl5bedrockapachelibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockapachelibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockapachelibdir)" || exit $$?; \
	done

uninstall-perl5bedrockapachelibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockapachelib_DATA)'; test -n "$(perl5bedrockapachelibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockapachelibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockapachelibdir)" && rm -f $$files
install-perl5bedrockapplicationlibDATA: $(perl5bedrockapplicationlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockapplicationlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockapplicationlibdir)"
	@list='$(perl5bedrockapplicationlib_DATA)'; test -n "$(perl5bedrockapplicationlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockapplicationlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockapplicationlibdir)" || exit $$?; \
	done

uninstall-perl5bedrockapplicationlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockapplicationlib_DATA)'; test -n "$(perl5bedrockapplicationlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockapplicationlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockapplicationlibdir)" && rm -f $$files
install-perl5bedrockcontextlibDATA: $(perl5bedrockcontextlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockcontextlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockcontextlibdir)"
	@list='$(perl5bedrockcontextlib_DATA)'; test -n "$(perl5bedrockcontextlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockcontextlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockcontextlibdir)" || exit $$?; \
	done

uninstall-perl5bedrockcontextlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockcontextlib_DATA)'; test -n "$(perl5bedrockcontextlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockcontextlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockcontextlibdir)" && rm -f $$files
install-perl5bedrockhtmllibDATA: $(perl5bedrockhtmllib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockhtmllibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockhtmllibdir)"
	@list='$(perl5bedrockhtmllib_DATA)'; test -n "$(perl5bedrockhtmllibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockhtmllibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockhtmllibdir)" || exit $$?; \
	done

uninstall-perl5bedrockhtmllibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockhtmllib_DATA)'; test -n "$(perl5bedrockhtmllibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockhtmllibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockhtmllibdir)" && rm -f $$files
install-perl5bedrockinputsourcelibDATA: $(perl5bedrockinputsourcelib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockinputsourcelibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockinputsourcelibdir)"
	@list='$(perl5bedrockinputsourcelib_DATA)'; test -n "$(perl5bedrockinputsourcelibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockinputsourcelibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockinputsourcelibdir)" || exit $$?; \
	done

uninstall-perl5bedrockinputsourcelibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockinputsourcelib_DATA)'; test -n "$(perl5bedrockinputsourcelibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockinputsourcelibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockinputsourcelibdir)" && rm -f $$files
install-perl5bedrocklibDATA: $(perl5bedrocklib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocklibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocklibdir)"
	@list='$(perl5bedrocklib_DATA)'; test -n "$(perl5bedrocklibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocklibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocklibdir)" || exit $$?; \
	done

uninstall-perl5bedrocklibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocklib_DATA)'; test -n "$(perl5bedrocklibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocklibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocklibdir)" && rm -f $$files
install-perl5bedrocktextlibDATA: $(perl5bedrocktextlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktextlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktextlibdir)"
	@list='$(perl5bedrocktextlib_DATA)'; test -n "$(perl5bedrocktextlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktextlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktextlibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktextlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktextlib_DATA)'; test -n "$(perl5bedrocktextlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktextlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktextlibdir)" && rm -f $$files
install-perl5bedrocktexttagxlibDATA: $(perl5bedrocktexttagxlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxlibdir)"
	@list='$(perl5bedrocktexttagxlib_DATA)'; test -n "$(perl5bedrocktexttagxlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxlibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxlib_DATA)'; test -n "$(perl5bedrocktexttagxlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxlibdir)" && rm -f $$files
install-perl5bedrocktexttagxtaglibDATA: $(perl5bedrocktexttagxtaglib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxtaglibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)"
	@list='$(perl5bedrocktexttagxtaglib_DATA)'; test -n "$(perl5bedrocktexttagxtaglibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxtaglibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxtaglib_DATA)'; test -n "$(perl5bedrocktexttagxtaglibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)" && rm -f $$files
install-perl5bedrocktexttagxtagnobodylibDATA: $(perl5bedrocktexttagxtagnobodylib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxtagnobodylibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)"
	@list='$(perl5bedrocktexttagxtagnobodylib_DATA)'; test -n "$(perl5bedrocktexttagxtagnobodylibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxtagnobodylibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxtagnobodylib_DATA)'; test -n "$(perl5bedrocktexttagxtagnobodylibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)" && rm -f $$files
install-perl5bedrocktexttagxtagwithbodyiflibDATA: $(perl5bedrocktexttagxtagwithbodyiflib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxtagwithbodyiflibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)"
	@list='$(perl5bedrocktexttagxtagwithbodyiflib_DATA)'; test -n "$(perl5bedrocktexttagxtagwithbodyiflibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxtagwithbodyiflibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxtagwithbodyiflib_DATA)'; test -n "$(perl5bedrocktexttagxtagwithbodyiflibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)" && rm -f $$files
install-perl5bedrocktexttagxtagwithbodylibDATA: $(perl5bedrocktexttagxtagwithbodylib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxtagwithbodylibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)"
	@list='$(perl5bedrocktexttagxtagwithbodylib_DATA)'; test -n "$(perl5bedrocktexttagxtagwithbodylibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxtagwithbodylibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxtagwithbodylib_DATA)'; test -n "$(perl5bedrocktexttagxtagwithbodylibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)" && rm -f $$files
install-perl5bedrocktexttagxtagwithbodylooplibDATA: $(perl5bedrocktexttagxtagwithbodylooplib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxtagwithbodylooplibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)"
	@list='$(perl5bedrocktexttagxtagwithbodylooplib_DATA)'; test -n "$(perl5bedrocktexttagxtagwithbodylooplibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxtagwithbodylooplibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxtagwithbodylooplib_DATA)'; test -n "$(perl5bedrocktexttagxtagwithbodylooplibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)" && rm -f $$files
install-perl5bedrocktexttagxtermlibDATA: $(perl5bedrocktexttagxtermlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrocktexttagxtermlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)"
	@list='$(perl5bedrocktexttagxtermlib_DATA)'; test -n "$(perl5bedrocktexttagxtermlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)" || exit $$?; \
	done

uninstall-perl5bedrocktexttagxtermlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrocktexttagxtermlib_DATA)'; test -n "$(perl5bedrocktexttagxtermlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)" && rm -f $$files
install-perl5bedrockxmlcontainerlibDATA: $(perl5bedrockxmlcontainerlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockxmlcontainerlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)"
	@list='$(perl5bedrockxmlcontainerlib_DATA)'; test -n "$(perl5bedrockxmlcontainerlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)" || exit $$?; \
	done

uninstall-perl5bedrockxmlcontainerlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockxmlcontainerlib_DATA)'; test -n "$(perl5bedrockxmlcontainerlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)" && rm -f $$files
install-perl5bedrockxmllibDATA: $(perl5bedrockxmllib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5bedrockxmllibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5bedrockxmllibdir)"
	@list='$(perl5bedrockxmllib_DATA)'; test -n "$(perl5bedrockxmllibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5bedrockxmllibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5bedrockxmllibdir)" || exit $$?; \
	done

uninstall-perl5bedrockxmllibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5bedrockxmllib_DATA)'; test -n "$(perl5bedrockxmllibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5bedrockxmllibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5bedrockxmllibdir)" && rm -f $$files
install-perl5blmfilterlibDATA: $(perl5blmfilterlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5blmfilterlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5blmfilterlibdir)"
	@list='$(perl5blmfilterlib_DATA)'; test -n "$(perl5blmfilterlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5blmfilterlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5blmfilterlibdir)" || exit $$?; \
	done

uninstall-perl5blmfilterlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5blmfilterlib_DATA)'; test -n "$(perl5blmfilterlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5blmfilterlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5blmfilterlibdir)" && rm -f $$files
install-perl5blmindexedtablehandlerlibDATA: $(perl5blmindexedtablehandlerlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5blmindexedtablehandlerlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)"
	@list='$(perl5blmindexedtablehandlerlib_DATA)'; test -n "$(perl5blmindexedtablehandlerlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)" || exit $$?; \
	done

uninstall-perl5blmindexedtablehandlerlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5blmindexedtablehandlerlib_DATA)'; test -n "$(perl5blmindexedtablehandlerlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)" && rm -f $$files
install-perl5blmlibDATA: $(perl5blmlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5blmlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5blmlibdir)"
	@list='$(perl5blmlib_DATA)'; test -n "$(perl5blmlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5blmlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5blmlibdir)" || exit $$?; \
	done

uninstall-perl5blmlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5blmlib_DATA)'; test -n "$(perl5blmlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5blmlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5blmlibdir)" && rm -f $$files
install-perl5blmstartuplibDATA: $(perl5blmstartuplib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5blmstartuplibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5blmstartuplibdir)"
	@list='$(perl5blmstartuplib_DATA)'; test -n "$(perl5blmstartuplibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5blmstartuplibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5blmstartuplibdir)" || exit $$?; \
	done

uninstall-perl5blmstartuplibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5blmstartuplib_DATA)'; test -n "$(perl5blmstartuplibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5blmstartuplibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5blmstartuplibdir)" && rm -f $$files
install-perl5blmstartupsessionlibDATA: $(perl5blmstartupsessionlib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5blmstartupsessionlibdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5blmstartupsessionlibdir)"
	@list='$(perl5blmstartupsessionlib_DATA)'; test -n "$(perl5blmstartupsessionlibdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5blmstartupsessionlibdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5blmstartupsessionlibdir)" || exit $$?; \
	done

uninstall-perl5blmstartupsessionlibDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5blmstartupsessionlib_DATA)'; test -n "$(perl5blmstartupsessionlibdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5blmstartupsessionlibdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5blmstartupsessionlibdir)" && rm -f $$files
install-perl5libDATA: $(perl5lib_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(perl5libdir)" || $(MKDIR_P) "$(DESTDIR)$(perl5libdir)"
	@list='$(perl5lib_DATA)'; test -n "$(perl5libdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(perl5libdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(perl5libdir)" || exit $$?; \
	done

uninstall-perl5libDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(perl5lib_DATA)'; test -n "$(perl5libdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(perl5libdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(perl5libdir)" && rm -f $$files
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(perl5bedrockapachelibdir)" "$(DESTDIR)$(perl5bedrockapplicationlibdir)" "$(DESTDIR)$(perl5bedrockcontextlibdir)" "$(DESTDIR)$(perl5bedrockhtmllibdir)" "$(DESTDIR)$(perl5bedrockinputsourcelibdir)" "$(DESTDIR)$(perl5bedrocklibdir)" "$(DESTDIR)$(perl5bedrocktextlibdir)" "$(DESTDIR)$(perl5bedrocktexttagxlibdir)" "$(DESTDIR)$(perl5bedrocktexttagxtaglibdir)" "$(DESTDIR)$(perl5bedrocktexttagxtagnobodylibdir)" "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodyiflibdir)" "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylibdir)" "$(DESTDIR)$(perl5bedrocktexttagxtagwithbodylooplibdir)" "$(DESTDIR)$(perl5bedrocktexttagxtermlibdir)" "$(DESTDIR)$(perl5bedrockxmlcontainerlibdir)" "$(DESTDIR)$(perl5bedrockxmllibdir)" "$(DESTDIR)$(perl5blmfilterlibdir)" "$(DESTDIR)$(perl5blmindexedtablehandlerlibdir)" "$(DESTDIR)$(perl5blmlibdir)" "$(DESTDIR)$(perl5blmstartuplibdir)" "$(DESTDIR)$(perl5blmstartupsessionlibdir)" "$(DESTDIR)$(perl5libdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-local mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-perl5bedrockapachelibDATA \
	install-perl5bedrockapplicationlibDATA \
	install-perl5bedrockcontextlibDATA \
	install-perl5bedrockhtmllibDATA \
	install-perl5bedrockinputsourcelibDATA \
	install-perl5bedrocklibDATA install-perl5bedrocktextlibDATA \
	install-perl5bedrocktexttagxlibDATA \
	install-perl5bedrocktexttagxtaglibDATA \
	install-perl5bedrocktexttagxtagnobodylibDATA \
	install-perl5bedrocktexttagxtagwithbodyiflibDATA \
	install-perl5bedrocktexttagxtagwithbodylibDATA \
	install-perl5bedrocktexttagxtagwithbodylooplibDATA \
	install-perl5bedrocktexttagxtermlibDATA \
	install-perl5bedrockxmlcontainerlibDATA \
	install-perl5bedrockxmllibDATA install-perl5blmfilterlibDATA \
	install-perl5blmindexedtablehandlerlibDATA \
	install-perl5blmlibDATA install-perl5blmstartuplibDATA \
	install-perl5blmstartupsessionlibDATA install-perl5libDATA

install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-perl5bedrockapachelibDATA \
	uninstall-perl5bedrockapplicationlibDATA \
	uninstall-perl5bedrockcontextlibDATA \
	uninstall-perl5bedrockhtmllibDATA \
	uninstall-perl5bedrockinputsourcelibDATA \
	uninstall-perl5bedrocklibDATA \
	uninstall-perl5bedrocktextlibDATA \
	uninstall-perl5bedrocktexttagxlibDATA \
	uninstall-perl5bedrocktexttagxtaglibDATA \
	uninstall-perl5bedrocktexttagxtagnobodylibDATA \
	uninstall-perl5bedrocktexttagxtagwithbodyiflibDATA \
	uninstall-perl5bedrocktexttagxtagwithbodylibDATA \
	uninstall-perl5bedrocktexttagxtagwithbodylooplibDATA \
	uninstall-perl5bedrocktexttagxtermlibDATA \
	uninstall-perl5bedrockxmlcontainerlibDATA \
	uninstall-perl5bedrockxmllibDATA \
	uninstall-perl5blmfilterlibDATA \
	uninstall-perl5blmindexedtablehandlerlibDATA \
	uninstall-perl5blmlibDATA uninstall-perl5blmstartuplibDATA \
	uninstall-perl5blmstartupsessionlibDATA uninstall-perl5libDATA

.MAKE: install-am install-strip

.PHONY: all all-am check check-am clean clean-generic clean-local \
	distclean distclean-generic distdir dvi dvi-am html html-am \
	info info-am install install-am install-data install-data-am \
	install-dvi install-dvi-am install-exec install-exec-am \
	install-html install-html-am install-info install-info-am \
	install-man install-pdf install-pdf-am \
	install-perl5bedrockapachelibDATA \
	install-perl5bedrockapplicationlibDATA \
	install-perl5bedrockcontextlibDATA \
	install-perl5bedrockhtmllibDATA \
	install-perl5bedrockinputsourcelibDATA \
	install-perl5bedrocklibDATA install-perl5bedrocktextlibDATA \
	install-perl5bedrocktexttagxlibDATA \
	install-perl5bedrocktexttagxtaglibDATA \
	install-perl5bedrocktexttagxtagnobodylibDATA \
	install-perl5bedrocktexttagxtagwithbodyiflibDATA \
	install-perl5bedrocktexttagxtagwithbodylibDATA \
	install-perl5bedrocktexttagxtagwithbodylooplibDATA \
	install-perl5bedrocktexttagxtermlibDATA \
	install-perl5bedrockxmlcontainerlibDATA \
	install-perl5bedrockxmllibDATA install-perl5blmfilterlibDATA \
	install-perl5blmindexedtablehandlerlibDATA \
	install-perl5blmlibDATA install-perl5blmstartuplibDATA \
	install-perl5blmstartupsessionlibDATA install-perl5libDATA \
	install-ps install-ps-am install-strip installcheck \
	installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic pdf \
	pdf-am ps ps-am uninstall uninstall-am \
	uninstall-perl5bedrockapachelibDATA \
	uninstall-perl5bedrockapplicationlibDATA \
	uninstall-perl5bedrockcontextlibDATA \
	uninstall-perl5bedrockhtmllibDATA \
	uninstall-perl5bedrockinputsourcelibDATA \
	uninstall-perl5bedrocklibDATA \
	uninstall-perl5bedrocktextlibDATA \
	uninstall-perl5bedrocktexttagxlibDATA \
	uninstall-perl5bedrocktexttagxtaglibDATA \
	uninstall-perl5bedrocktexttagxtagnobodylibDATA \
	uninstall-perl5bedrocktexttagxtagwithbodyiflibDATA \
	uninstall-perl5bedrocktexttagxtagwithbodylibDATA \
	uninstall-perl5bedrocktexttagxtagwithbodylooplibDATA \
	uninstall-perl5bedrocktexttagxtermlibDATA \
	uninstall-perl5bedrockxmlcontainerlibDATA \
	uninstall-perl5bedrockxmllibDATA \
	uninstall-perl5blmfilterlibDATA \
	uninstall-perl5blmindexedtablehandlerlibDATA \
	uninstall-perl5blmlibDATA uninstall-perl5blmstartuplibDATA \
	uninstall-perl5blmstartupsessionlibDATA uninstall-perl5libDATA


@do_subst_command@

%.pm: %.pm.in
	test -d "$$(dirname "$@")" || $(INSTALL) -d "$$(dirname "$@")"
	$(do_subst) $< > $@

tidy: $(TIDY)

$(TIDY): %.in.tdy : %
	test -e $<.in && perltidy -vt=2 -pt=1 -dws -i=2 -lp -ci=0 -cti=1 $<.in || true
	test -e $@ && cp $@ $<.in && touch $@ || true

clean-local:
	find . -name '*.pm' -exec rm {} \;
	rm -rf *.tdy

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
