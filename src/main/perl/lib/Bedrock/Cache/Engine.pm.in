package Bedrock::Cache::Engine;

use Role::Tiny;

# The Contract: Every engine MUST implement these 5 methods
requires 'get';     # Retrieve raw data string
requires 'set';     # Store raw data string with optional TTL
requires 'exists';  # Boolean check for key presence
requires 'delete';  # Remove a specific key

1;
