#!/usr/bin/perl -w

# ident "@(#) $Header$"
# ident "@(#)        $Name$"

package TagX::TAG::NoBody::Include;

use strict;

BEGIN
{
    use vars	qw ($VERSION @ISA);
    $VERSION	= do { my @r = (q$Revision$ =~ /\d+/g); sprintf "%d."."%02d" x $#r, @r };
}

use Getopt::Long;
use POSIX qw (strftime);

use TagX::TAG::NoBody;

@ISA = qw (TagX::TAG::NoBody);

sub finalize
{
    my ($self, $tx, @context) = @_;

    local (@ARGV) = @{$self->{'argv'}};
    my %valid;
    &GetOptions(\%valid,
		'verbose',
		'file=s',
		'access-time=s',
		'access-time-format=s',
		'modify-time=s',
		'modify-time-format=s',
		'change-time=s',
		'change-time-format=s');

    my $verbose = delete $valid{'verbose'};

    if ( $verbose ) {
	my ($key, $value);
	my $str = "$self: Options are\n";
	while (($key, $value) = each %valid) {
	    $str .= "\t$key .. ($value)\n";
	}
	$tx->log_message( MSG_DEBUG => $str );
    }

    unless ( exists $valid{'file'} ) {
	$tx->log_message( MSG_DEBUG => "$self: No file specified" ) if $verbose;
	return;
    }

    my @sv = stat ( $valid{'file'} );
    return unless @sv;
    my $input = IO::File->new ( $valid{'file'} );
    return unless $input;
    my $subform = $tx->new ( $input );

    if ( exists $valid{'access-time'} ) {
	my $value = $sv[8];
	$value = &strftime ( $valid{'access-time-format'}, localtime( $value ) )
	    if exists $valid{'access-time-format'};
	$subform->param ( $valid{'access-time'}, $value );
    }

    if ( exists $valid{'modify-time'} ) {
	my $value = $sv[9];
	$value = &strftime ( $valid{'modify-time-format'}, localtime( $value ) )
	    if exists $valid{'modify-time-format'};
	$subform->param ( $valid{'modify-time'}, $value );
    }

    if ( exists $valid{'change-time'} ) {
	my $value = $sv[10];
	$value = &strftime ( $valid{'change-time-format'}, localtime( $value ) )
	    if exists $valid{'change-time-format'};
	$subform->param ( $valid{'change-time'}, $value );
    }

    $subform->output;
}

#
# Name of Release: $Name$
# $Log$
# Revision 1.3  2000/03/06 18:32:00  sridhar
# Replaced all ad-hoc message logging with message logging only on demand
# -- via --verbose flag specified as a tag option
#
# Revision 1.2  2000/02/08 15:37:44  sridhar
# Added support for being able to use Exporter version numbers
#
# Revision 1.1  2000/02/08 15:24:58  sridhar
# Added TagX
#
#

1;
