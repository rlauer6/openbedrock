--- 
name: <snippet>
test: |
  <snippet --verbose --verbose "this is a test">
  use Digest::MD5 qw/md5/;
  use MIME::Base64;
  
  return encode_base64(md5(shift));
  </snippet>
  <var $_>
result: /^\s*VLDF/
op: like

