# - Apache2 mod_perl script to be run during Apache startup for example by specifying:
#
#   PerlRequire /var/www/config/startup.pl
#
BEGIN {
  use lib '@libdir@/bedrock/perl5';
}

use Apache2::compat;

use Bedrock;
use Text::TagX;
use POSIX;

1;
